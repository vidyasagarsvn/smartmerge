<UserControl x:Class="SmartMerge.Views.FolderCompareView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:SmartMerge.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:FolderCompareViewModel}">
    <Grid Background="{DynamicResource AppBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="12,12,12,0" VerticalAlignment="Center">
            <TextBlock Text="Filter by status:" Foreground="{DynamicResource TextForegroundBrush}" VerticalAlignment="Center" Margin="0,0,12,0" />
            <CheckBox Content="Identical" IsChecked="{Binding FilterIdentical}" Margin="0,0,12,0" />
            <CheckBox Content="Modified" IsChecked="{Binding FilterModified}" Margin="0,0,12,0" />
            <CheckBox Content="Left Only" IsChecked="{Binding FilterLeftOnly}" Margin="0,0,12,0" />
            <CheckBox Content="Right Only" IsChecked="{Binding FilterRightOnly}" Margin="0,0,12,0" />
            <CheckBox Content="Missing" IsChecked="{Binding FilterMissing}" />
        </StackPanel>

        <Border Grid.Row="1" Margin="12" Background="{DynamicResource PanelBackgroundBrush}" BorderBrush="{DynamicResource PanelBorderBrush}" BorderThickness="1">
            <DataGrid x:Name="ItemsGrid"
                      ItemsSource="{Binding Items}"
                      AutoGenerateColumns="False"
                      HeadersVisibility="Column"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      CanUserResizeRows="False"
                      IsReadOnly="True"
                      GridLinesVisibility="Horizontal">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Width="*">
                        <DataGridTextColumn.Binding>
                            <MultiBinding Converter="{StaticResource FolderNameConverter}">
                                <Binding Path="Name" />
                                <Binding Path="Type" />
                            </MultiBinding>
                        </DataGridTextColumn.Binding>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Status" Width="200" Binding="{Binding Status, Converter={StaticResource FolderStatusToTextConverter}}" />
                </DataGrid.Columns>
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Status}" Value="FolderLeftOnly">
                                <Setter Property="Foreground" Value="{DynamicResource SubtleTextBrush}" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Status}" Value="FolderRightOnly">
                                <Setter Property="Foreground" Value="{DynamicResource SubtleTextBrush}" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>

<UserControl x:Class="SmartMerge.Views.FileCompareView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:SmartMerge.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:FileCompareViewModel}">
    <UserControl.Resources>
        <Style TargetType="ListViewItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsInCurrentRegion}" Value="True">
                    <Setter Property="Background" Value="{DynamicResource RegionHighlightBrush}" />
                </DataTrigger>
                <DataTrigger Binding="{Binding IsSearchMatch}" Value="True">
                    <Setter Property="Background" Value="{DynamicResource SearchHighlightBrush}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Background="{DynamicResource AppBackgroundBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="8" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="36" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Grid.Column="0" Background="{DynamicResource HeaderBackgroundBrush}" BorderBrush="{DynamicResource PanelBorderBrush}" BorderThickness="0,0,0,1">
            <TextBlock Text="{Binding LeftFileLabel}"
                       Foreground="{DynamicResource HeaderForegroundBrush}"
                       FontWeight="SemiBold"
                       FontSize="12"
                       VerticalAlignment="Center"
                       Margin="12,0" />
        </Border>

        <Border Grid.Row="0" Grid.Column="2" Background="{DynamicResource HeaderBackgroundBrush}" BorderBrush="{DynamicResource PanelBorderBrush}" BorderThickness="0,0,0,1">
            <TextBlock Text="{Binding RightFileLabel}"
                       Foreground="{DynamicResource HeaderForegroundBrush}"
                       FontWeight="SemiBold"
                       FontSize="12"
                       VerticalAlignment="Center"
                       Margin="12,0" />
        </Border>

        <GridSplitter Grid.Row="0" Grid.Column="1" Grid.RowSpan="2" Width="8" Background="{DynamicResource PanelBorderBrush}" HorizontalAlignment="Stretch" />

        <Border Grid.Row="1" Grid.Column="0" Background="{DynamicResource PanelBackgroundBrush}" BorderBrush="{DynamicResource PanelBorderBrush}" BorderThickness="1,0,1,1" Margin="12,12,6,12">
            <ListView x:Name="LeftList"
                      ItemsSource="{Binding Rows}"
                      ScrollViewer.HorizontalScrollBarVisibility="Auto"
                      ScrollViewer.VerticalScrollBarVisibility="Auto"
                      BorderThickness="0">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="60" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Border Grid.Column="0" Background="{DynamicResource LineNumberBackgroundBrush}" Padding="6,2">
                                <TextBlock Text="{Binding LeftLineNumber}"
                                           Foreground="{DynamicResource LineNumberForegroundBrush}"
                                           FontFamily="{Binding DataContext.DiffFontFamily, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                           FontSize="{Binding DataContext.DiffFontSize, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                            </Border>
                            <Border Grid.Column="1" Background="{Binding LeftKind, Converter={StaticResource DiffKindToBrushConverter}}" Padding="6,2">
                                <TextBlock Text="{Binding LeftText}"
                                           Foreground="{DynamicResource TextForegroundBrush}"
                                           FontFamily="{Binding DataContext.DiffFontFamily, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                           FontSize="{Binding DataContext.DiffFontSize, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                            </Border>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>

        <Border Grid.Row="1" Grid.Column="2" Background="{DynamicResource PanelBackgroundBrush}" BorderBrush="{DynamicResource PanelBorderBrush}" BorderThickness="1,0,1,1" Margin="6,12,12,12">
            <ListView x:Name="RightList"
                      ItemsSource="{Binding Rows}"
                      ScrollViewer.HorizontalScrollBarVisibility="Auto"
                      ScrollViewer.VerticalScrollBarVisibility="Auto"
                      BorderThickness="0">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="60" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Border Grid.Column="0" Background="{DynamicResource LineNumberBackgroundBrush}" Padding="6,2">
                                <TextBlock Text="{Binding RightLineNumber}"
                                           Foreground="{DynamicResource LineNumberForegroundBrush}"
                                           FontFamily="{Binding DataContext.DiffFontFamily, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                           FontSize="{Binding DataContext.DiffFontSize, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                            </Border>
                            <Border Grid.Column="1" Background="{Binding RightKind, Converter={StaticResource DiffKindToBrushConverter}}" Padding="6,2">
                                <TextBlock Text="{Binding RightText}"
                                           Foreground="{DynamicResource TextForegroundBrush}"
                                           FontFamily="{Binding DataContext.DiffFontFamily, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                           FontSize="{Binding DataContext.DiffFontSize, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                            </Border>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>
    </Grid>
</UserControl>

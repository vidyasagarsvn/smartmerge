<Window x:Class="SmartMerge.Views.Dialogs.OpenFilesDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Open Files for Comparison"
        Height="300"
        Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource AppBackgroundBrush}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="Left File:" Margin="0,0,0,6" FontWeight="SemiBold" />
        <TextBox Grid.Row="1" Grid.Column="0" x:Name="LeftPathBox" IsReadOnly="True" Height="32" Margin="0,0,12,6" />
        <Button Grid.Row="1" Grid.Column="1" Content="Browse..." Width="100" Height="32" Click="BrowseLeft" />
        <TextBlock Grid.Row="2" Grid.ColumnSpan="2" x:Name="LeftStatus" Foreground="{DynamicResource SubtleTextBrush}" FontSize="11" Margin="0,0,0,12" />

        <TextBlock Grid.Row="3" Grid.ColumnSpan="2" Text="Right File:" Margin="0,0,0,6" FontWeight="SemiBold" />
        <TextBox Grid.Row="4" Grid.Column="0" x:Name="RightPathBox" IsReadOnly="True" Height="32" Margin="0,0,12,6" />
        <Button Grid.Row="4" Grid.Column="1" Content="Browse..." Width="100" Height="32" Click="BrowseRight" />
        <TextBlock Grid.Row="5" Grid.ColumnSpan="2" x:Name="RightStatus" Foreground="{DynamicResource SubtleTextBrush}" FontSize="11" Margin="0,0,0,12" />

        <StackPanel Grid.Row="6" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Cancel" Width="100" Margin="0,0,12,0" Click="Cancel" />
            <Button x:Name="CompareButton" Content="Compare" Width="100" IsEnabled="False" Click="Compare" />
        </StackPanel>
    </Grid>
</Window>

<Window x:Class="SmartMerge.Views.Dialogs.SearchDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Find"
        Height="160"
        Width="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource AppBackgroundBrush}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Find:" VerticalAlignment="Center" Margin="0,0,8,0" />
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="SearchBox" Height="32" KeyDown="OnSearchKeyDown" />

        <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,12,0,0">
            <Button Content="Previous" Width="110" Height="32" Margin="0,0,8,0" Click="OnFindPrevious" />
            <Button Content="Next" Width="110" Height="32" Margin="0,0,16,0" Click="OnFindNext" />
            <CheckBox x:Name="CaseSensitiveBox" Content="Case sensitive" VerticalAlignment="Center" Margin="0,0,16,0" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Close" Width="80" Height="32" Click="OnClose" />
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
